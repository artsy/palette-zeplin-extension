{
  "name": "zeplin-palette",
  "version": "0.1.0",
  "description": "Sample extension description.",
  "license": "MIT",
  "scripts": {
    "start": "zem start",
    "build": "zem build && cp dist/* docs",
    "clean": "zem clean",
    "exec": "zem exec"
  },
  "zeplin": {
    "displayName": "Palette",
    "projectTypes": [
      "web"
    ]
  },
  "dependencies": {
    "@artsy/palette": "^2.0.1",
    "color": "^3.0.0",
    "lodash": "^4.17.10",
    "zem": "^0.1.2"
  }
}
